# Migration `20190914082348-reports`

This migration has been generated by Andreas Møller at 9/14/2019, 8:23:48 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190911225035-init..20190914082348-reports
--- datamodel.dml
+++ datamodel.dml
@@ -43,7 +43,7 @@
   createdAt    DateTime @default(now())
   email        String   @unique
   firstName    String
   lastName     String
-  report       Report[]
+  reports      Report[]
   refreshToken String?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190914082348-reports)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190914082348-reports'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
