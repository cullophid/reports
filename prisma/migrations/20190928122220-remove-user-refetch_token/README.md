# Migration `20190928122220-remove-user-refetch_token`

This migration has been generated by Andreas Møller at 9/28/2019, 12:22:20 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `reports`.`User` DROP COLUMN `refreshToken`;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190924234158-slides..20190928122220-remove-user-refetch_token
--- datamodel.dml
+++ datamodel.dml
@@ -22,12 +22,11 @@
   report Report
 }
 model User {
-  id           String   @default(cuid()) @id
-  createdAt    DateTime @default(now())
-  email        String   @unique
-  firstName    String
-  lastName     String
-  reports      Report[]
-  refreshToken String?
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  email     String   @unique
+  firstName String
+  lastName  String
+  reports   Report[]
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190928122220-remove-user-refetch_token)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190928122220-remove-user-refetch_token'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
