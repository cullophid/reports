# Migration `20190914092252-restart`

This migration has been generated by Andreas Møller at 9/14/2019, 9:22:52 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE `reports`.`Node`;

DROP TABLE `reports`.`NodeChart`;

DROP TABLE `reports`.`Slide`;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190914091715-user_reports..20190914092252-restart
--- datamodel.dml
+++ datamodel.dml
@@ -8,43 +8,14 @@
 }
 model Report {
-  id     String  @default(cuid()) @id
-  owner  User?
-  slides Slide[]
-  title  String
+  id    String @default(cuid()) @id
+  owner User?
+  title String
 }
-model Slide {
-  id     String  @default(cuid()) @id
-  nodes  Node[]
-  report Report?
-}
-model Node {
-  id     String     @default(cuid()) @id
-  chart  NodeChart?
-  height Int
-  slide  Slide?
-  text   String?
-  type   NodeType
-  width  Int
-  x      Int
-  y      Int
-}
-
-enum NodeType {
-  Text
-  Chart
-}
-
-model NodeChart {
-  id    String @default(cuid()) @id
-  node  Node
-  query String
-}
-
 model User {
   id           String   @default(cuid()) @id
   createdAt    DateTime @default(now())
   email        String   @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190914092252-restart)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190914092252-restart'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
